import { useMemo, useState } from 'react'
import { createRoot } from 'react-dom/client'
import { Background, Controls, Handle, MiniMap, Position, ReactFlow, type Edge, type Node, type NodeProps } from '@xyflow/react'
import '@xyflow/react/dist/style.css'
import { BookOpen, Check, ChevronRight, FlaskConical, Lock, Play, Target, X, Zap } from 'lucide-react'
import './styles.css'

type Status = 'mastered' | 'learning' | 'not-started'
type Category = 'Foundations' | 'Networking' | 'Compute' | 'Storage' | 'Identity' | 'Databases' | 'Infrastructure' | 'Containers' | 'DevOps' | 'Security' | 'Observability' | 'Governance' | 'Reliability' | 'Architecture'
type Topic = { id: string; title: string; category: Category; status: Status; progress: number; x: number; y: number; prereqs: string[]; major?: boolean; description: string }
const tint: Record<Category, string> = { Foundations: '#16161a', Networking: '#101923', Compute: '#171523', Storage: '#132019', Identity: '#20191e', Databases: '#191622', Infrastructure: '#181822', Containers: '#182019', DevOps: '#151c20', Security: '#201813', Observability: '#1d1715', Governance: '#171c24', Reliability: '#20181a', Architecture: '#1d1924' }
const color: Record<Status, string> = { mastered: '#22c55e', learning: '#0ea5e9', 'not-started': '#52525b' }
const name: Record<Status, string> = { mastered: 'Mastered', learning: 'Learning', 'not-started': 'Not Started' }
const levels: [string, Category, string[], Status?][] = [
    ['Foundations', 'Foundations', ['Cloud Concepts', 'Shared Responsibility', 'Cloud Economics', 'Azure Core'], 'mastered'],
    ['Core Cloud Engineer', 'Networking', ['Virtual Network', 'Network Security Groups', 'Blob Storage', 'Microsoft Entra ID', 'Azure SQL'], 'learning'],
    ['Advanced Cloud Engineer', 'Compute', ['Virtual Machines', 'App Service', 'Azure Functions', 'Bicep', 'Terraform', 'Container Registry', 'AKS Cluster Operations']],
    ['Senior Cloud Engineer', 'DevOps', ['CI/CD Pipelines', 'GitOps', 'Defender for Cloud', 'Key Vault', 'Azure Monitor', 'SLOs & Alerting', 'Landing Zones']],
    ['Cloud Architect', 'Architecture', ['Well-Architected Framework', 'Reference Architectures', 'Disaster Recovery', 'Cost Management', 'Azure Policy']]
]
const initial: string[] = []
const categoryFor=(title:string,fallback:Category):Category=>({ 'Blob Storage':'Storage','Microsoft Entra ID':'Identity','Azure SQL':'Databases','Bicep':'Infrastructure','Terraform':'Infrastructure','Container Registry':'Containers','AKS Cluster Operations':'Containers','Defender for Cloud':'Security','Key Vault':'Security','Azure Monitor':'Observability','SLOs & Alerting':'Observability','Landing Zones':'Governance','Cost Management':'Governance','Azure Policy':'Governance','Disaster Recovery':'Reliability' }[title] as Category)||fallback
const topics: Topic[] = levels.flatMap(([title, category, children, state = 'not-started'], i) => { const x = i * 620; const parent = { id: `level-${i}`, title, category, status: state, progress: state === 'mastered' ? 100 : state === 'learning' ? 58 : 0, x, y: 330, prereqs: initial.slice(), major: true, description: `Develop a practical foundation in ${title} with connected Azure concepts and hands-on learning.` } as Topic; initial.splice(0, initial.length, title); return [parent, ...children.map((child, j) => ({ id: `${i}-${j}`, title: child, category:categoryFor(child,category), status: state === 'mastered' ? 'mastered' : state === 'learning' && (j === 0 || j === 1) ? 'learning' : 'not-started', progress: state === 'mastered' ? 100 : state === 'learning' && (j === 0 || j === 1) ? (j === 0 ? 100 : 70) : 0, x:x+235, y:45+j*(580 / Math.max(children.length - 1, 1)), prereqs: [title], description: `Learn ${child} through theory, a knowledge check, a guided Azure lab, and an independent challenge.` } as Topic))] })
// Each stage branches out to every skill, then every skill converges into the next career stage.
topics.forEach((topic, index) => {
  if (topic.major && index > 0) {
    const stage = Number(topic.id.replace('level-', ''))
    topic.prereqs = topics.filter(candidate => candidate.id.startsWith(`${stage - 1}-`)).map(candidate => candidate.title)
  }
})
function TopicNode({ data }: NodeProps<Node<Topic>>) { const t = data as unknown as Topic; return <div className={`topic-node ${t.major ? 'major' : ''}`} style={{ backgroundColor: tint[t.category], borderColor: color[t.status] }}><Handle type="target" position={Position.Top} /><div className="node-meta"><i style={{ background: color[t.status] }} />{t.category}{t.status === 'not-started' && <Lock size={11} />}</div><strong>{t.title}</strong><div className="node-footer"><span>{name[t.status]}</span><span>{t.progress}%</span></div><div className="node-progress"><i style={{ width: `${t.progress}%`, background: color[t.status] }} /></div><Handle type="source" position={Position.Bottom} /></div> }
function App() { const [selected, setSelected] = useState(topics.find(t => t.status === 'learning')!); const [open, setOpen] = useState(true); const nodes = useMemo<Node<Topic>[]>(() => topics.map(t => ({ id: t.id, type: 'topic', position: { x: t.x, y: t.y }, data: t })), []); const edges = useMemo<Edge[]>(() => topics.flatMap(t => t.prereqs.map(p => { const source = topics.find(x => x.title === p)?.id; return source ? { id: `${source}-${t.id}`, source, target: t.id, type: 'smoothstep', style: { stroke: t.status === 'not-started' ? '#3f3f46' : color[t.status], strokeWidth: t.major ? 2 : 1.2 } } : null })).filter(Boolean) as Edge[], []); const phases = [['Theory', BookOpen], ['Quiz', Target], ['Guided Lab', FlaskConical], ['Independent Challenge', Zap]] as const; return <div className="app-shell"><section className="overview"><div><p className="eyebrow">CLOUD ABDULLAH - 403</p><h1>AZURE LEARNING ROADMAP</h1></div><div className="metrics"><div className="metric"><b>42%</b><p>Overall progress</p></div><div className="metric"><b>18</b><p>Topics mastered</p></div><div className="metric focus"><Play size={14} /><div><b>Network Security</b><p>Learning now</p></div></div></div></section><main className={open ? 'with-panel' : ''}><section className="canvas-wrap"><div className="canvas-title"><span>Skill map · vertically connected progression</span><div className="legend">{(['mastered', 'learning', 'not-started'] as Status[]).map(s => <i key={s}><b style={{ background: color[s] }} />{name[s]}</i>)}</div></div><ReactFlow nodes={nodes} edges={edges} nodeTypes={{ topic: TopicNode }} onNodeClick={(_, n) => { setSelected(n.data as Topic); setOpen(true) }} fitView minZoom={.18} maxZoom={1.4}><Background color="#27272a" gap={28} size={1} /><Controls showInteractive={false} /><MiniMap nodeColor={n => color[(n.data as Topic).status]} maskColor="rgba(9,9,11,.82)" /></ReactFlow><p className="canvas-hint">Scroll to zoom · drag to explore the roadmap</p></section>{open && <aside className="detail"><button className="close" onClick={() => setOpen(false)}><X size={18} /></button><div className="panel-status" style={{ color: color[selected.status] }}><i style={{ background: color[selected.status] }} />{name[selected.status]}</div><p className="panel-category">{selected.category}</p><h2>{selected.title}</h2><p className="description">{selected.description}</p><div className="panel-stat"><span>Topic progress</span><b>{selected.progress}%</b><div><i style={{ width: `${selected.progress}%`, background: color[selected.status] }} /></div></div><div className="prereqs"><span>Prerequisites</span><div>{selected.prereqs.length ? selected.prereqs.map(p => <button key={p}>{p}<ChevronRight size={13} /></button>) : <p>This is where your roadmap begins.</p>}</div></div><div className="path"><span>Mastery path</span>{phases.map(([label, Icon], i) => <div className="stage" key={label}><Icon size={15} /><span>{label}</span>{i < Math.ceil(selected.progress / 25) && <Check size={13} />}</div>)}</div><button className="continue">{selected.status === 'not-started' ? 'Start learning' : 'Continue learning'}<ChevronRight size={17} /></button></aside>}</main></div> }
createRoot(document.getElementById('root')!).render(<App />)
