import curriculum from './curriculum.md?raw'
import type { RoadmapTopic } from './types'

const slug = (value: string) => value.toLowerCase().replace(/&/g, 'and').replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '')
const list = (value: string) => [...value.matchAll(/^\* (.+)$/gm)].map(match => match[1])
const names: Record<string, string> = { FOUNDATIONS: 'Foundations', 'CORE CLOUD ENGINEER': 'Core Cloud Engineer', 'ADVANCED CLOUD ENGINEER': 'Advanced Cloud Engineer', 'SENIOR CLOUD ENGINEER': 'Senior Cloud Engineer', 'CLOUD ARCHITECT': 'Cloud Architect' }
const dependencyTitles: Record<string, string[]> = { 'Virtual Networks':['Networking Basics'], 'Subnets & Routing':['Networking Basics','Virtual Networks'], 'Microsoft Entra ID':['Azure Fundamentals'], 'Azure RBAC':['Microsoft Entra ID'], 'Managed Identities':['Azure RBAC'], 'NSG & ASG':['Virtual Networks','Subnets & Routing'], 'VNet Peering':['Virtual Networks'], 'Private Endpoints':['Virtual Networks','Azure DNS'], 'Blob Storage':['Storage Accounts'], 'Azure Files':['Storage Accounts'], 'Virtual Machines':['Linux Basics','Networking Basics','Azure Fundamentals'], 'Azure Container Registry':['Docker'], 'Kubernetes Fundamentals':['Docker'], 'Kubernetes Networking':['Kubernetes Fundamentals'], 'Kubernetes Storage':['Kubernetes Fundamentals'], 'Kubernetes Security':['Kubernetes Fundamentals'], 'AKS Architecture':['Kubernetes Fundamentals','Azure Container Registry','Virtual Networks'], 'AKS Networking':['AKS Architecture','Kubernetes Networking'], 'AKS Identity':['AKS Architecture','Managed Identities','Kubernetes Security'], 'AKS Scaling':['AKS Architecture'], 'AKS Operations':['AKS Architecture','Azure Monitor'], 'GitHub Actions':['Git & GitHub'], 'IaC Pipelines':['Terraform','Bicep','GitHub Actions'], 'KQL':['Azure Monitor','Log Analytics'], 'Centralized Logging':['Log Analytics'], 'Distributed Tracing':['Application Insights'], 'Alerting':['Azure Monitor'], 'Hub-Spoke Architecture':['Virtual Networks','Subnets & Routing','Azure Firewall'], 'Azure Landing Zones':['Management Groups','Azure Policy','Enterprise RBAC'], 'Platform Landing Zone':['Azure Landing Zones'], 'Application Landing Zones':['Azure Landing Zones'], 'Disaster Recovery':['High Availability','RTO & RPO'], 'SLI / SLO / SLA':['Azure Monitor','Application Insights','High Availability'], 'Reusable IaC Modules':['Terraform State & Modules'], 'Self-Service Infrastructure':['Reusable IaC Modules','Azure Policy'], 'Subscription Vending':['Azure Landing Zones','Reusable IaC Modules'], 'Azure Well-Architected Framework':['Architecture Fundamentals'], 'Multi-Region Architecture':['High Availability','Disaster Recovery'], 'Network Architecture':['Hub-Spoke Architecture','ExpressRoute','Azure Landing Zones'], 'Identity Architecture':['Microsoft Entra ID','Enterprise RBAC','Managed Identities'], 'Governance Architecture':['Management Groups','Azure Policy','Subscription Design'], 'Global Routing':['Multi-Region Architecture'], 'Regional Failover':['Multi-Region Architecture','RTO & RPO'], 'Cross-Region Data Replication':['SQL vs NoSQL','Multi-Region Architecture'] }
function parse(section:string, heading:string):RoadmapTopic[] { const level=names[heading]; return [...section.matchAll(/^## (.+?)\r?\n\r?\nlearnToComplete:\r?\n\r?\n([\s\S]*?)\r?\n\r?\npractice:\r?\n\r?\n([\s\S]*?)\r?\n\r?\nazureServices:\r?\n\r?\n([\s\S]*?)(?=\r?\n\r?\n## |\r?\n# |$)/gm)].map(match=>{ const title=match[1]; const category=level; return { id:`${slug(level)}-${slug(category)}-${slug(title)}`, title, description:'', level, category, prerequisites:[], difficulty:level, learnToComplete:list(match[2]), practice:list(match[3]), azureServices:list(match[4]) } }) }
const sections=[...curriculum.matchAll(/^# LEVEL \d+ — (.+?)\r?\n([\s\S]*?)(?=^# LEVEL |^# Dependency rules)/gm)]
const initial=sections.flatMap(match=>parse(match[2],match[1]))
const idByTitle=new Map(initial.map(topic=>[topic.title,topic.id]))
export const roadmapTopics:RoadmapTopic[]=initial.map(topic=>({...topic,prerequisites:(dependencyTitles[topic.title]??[]).map(title=>idByTitle.get(title)).filter((id):id is string=>Boolean(id))}))
